import Card from "../Card/Card";
import styles from "./Captcha.module.sass";
import { ICaptchaProps } from "./Captcha.types";

const Captcha = ({ onChange }: ICaptchaProps) => {
  return (
    <Card>
      <div></div>
    </Card>
  );
};

export default Captcha;
